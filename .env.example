# MongoDB Connection Configuration Template
# Copy this to .env and fill in your values
# Get the URI from MongoDB Atlas → Deployments → Connect → Drivers → Python

MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/?appName=cluster-name

# Database name
DB_NAME=cookie_clicker_67

# Flask Settings
FLASK_ENV=development
DEBUG=True
